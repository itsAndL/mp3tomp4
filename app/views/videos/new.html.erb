<!-- app/views/videos/new.html.erb -->
<h1 class="text-3xl font-bold text-gray-900 mb-6">Upload Video</h1>

<%= form_with url: videos_path, local: true, multipart: true, class: "bg-white shadow rounded-lg p-6" do |form| %>
  <div class="mb-4">
    <%= form.label :title, class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.text_field :title, class: "w-full border border-gray-300 rounded-md px-3 py-2" %>
  </div>
  
  <div class="mb-4">
    <%= form.label :description, class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.text_area :description, rows: 4, class: "w-full border border-gray-300 rounded-md px-3 py-2" %>
  </div>
  
  <div class="mb-6">
    <%= form.label :video_file, class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.file_field :video_file, accept: "video/*", class: "w-full border border-gray-300 rounded-md px-3 py-2" %>
  </div>
  
  <div class="mb-6">
    <%= form.label :privacy_status, "Privacy Status", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= form.select :privacy_status, 
        options_for_select([
          ['Public - Anyone can find and view', 'public'],
          ['Unlisted - Only people with the link can view', 'unlisted'],
          ['Private - Only you can view', 'private']
        ], 'public'), 
        {}, 
        { class: "w-full border border-gray-300 rounded-md px-3 py-2 focus:ring-blue-500 focus:border-blue-500" } %>
    <p class="mt-1 text-sm text-gray-500">Choose who can see your video</p>
  </div>

  <div class="flex justify-end">
    <%= link_to "Cancel", videos_path, class: "bg-gray-300 text-gray-700 px-4 py-2 rounded mr-2 hover:bg-gray-400" %>
    <%= form.submit "Upload Video", class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
  </div>
<% end %>